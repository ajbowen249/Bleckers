@page "/"
@using Bleckers.Components;

<GameStatus Model="Game" />

<Board Game="Game" />

@code {
    public GameModel Game = new GameModel { };
    protected override void OnInitialized() {
        base.OnInitialized();
        Game.StateChanged += (s, a) => StateHasChanged();
    }
}
